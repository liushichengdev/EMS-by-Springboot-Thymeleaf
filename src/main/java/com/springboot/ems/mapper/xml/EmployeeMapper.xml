<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.springboot.ems.mapper.EmployeeMapper">

    <select id="lists" resultType="com.springboot.ems.entity.Employee">
        select id, name, salary, birthday, photo from `employee`
    </select>

    <insert id="save" parameterType="com.springboot.ems.entity.Employee">
        insert into employee values (#{id}, #{name},#{salary}, #{birthday},#{photo})
    </insert>

    <select id="findById" parameterType="Integer" resultType="com.springboot.ems.entity.Employee">
        select * from employee where id=#{id}
    </select>

    <update id="update" parameterType="com.springboot.ems.entity.Employee">
        update `employee` set name=#{name},salary=#{salary},birthday=#{birthday},photo=#{photo}
        where id=#{id}
    </update>

    <delete id="delete" parameterType="Integer">
        delete from `employee` where id=#{id}
    </delete>
</mapper>